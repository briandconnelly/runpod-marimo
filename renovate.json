{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": ["config:recommended"],
  "customManagers": [
    {
      "customType": "regex",
      "description": "Dockerfile ARG versions annotated with renovate comments",
      "managerFilePatterns": [
        "/(^|/)Dockerfile$/"
      ],
      "matchStrings": [
        "#\\s*renovate:\\s*datasource=(?<datasource>\\S+)\\s+depName=(?<depName>\\S+)(?:\\s+versioning=(?<versioning>\\S+))?\\nARG\\s+\\w+=(?<currentValue>\\S+)"
      ]
    }
  ],
  "packageRules": [
    {
      "description": "Remind about SHA256 checksum updates for binary downloads",
      "matchDepNames": ["duckdb/duckdb", "runpod/runpodctl"],
      "prBodyNotes": [
        "This package uses SHA256 checksum verification in the Dockerfile. Download the new binary and update the corresponding `_SHA256` ARG before merging."
      ]
    },
    {
      "description": "Stay on the Python 3.13 series; do not propose 3.14+ upgrades",
      "matchDatasources": ["python-version"],
      "matchDepNames": ["python"],
      "allowedVersions": ">=3.13 <3.14"
    }
  ]
}
